{
  "name": "@espcom/esbuild-plugin-webpack-analyzer",
  "author": "espcom",
  "license": "MIT",
  "version": "0.0.3",
  "description": "Webpack Bundle Analyzer integration for esbuild",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/esbuild-plugins-community/esbuild-plugin-webpack-analyzer.git"
  },
  "scripts": {
    "prepare": "husky",
    "prepublishOnly": "pnpm run analyze:js&&pnpm run check-types&&pnpm run test",
    "analyze:js": "eslint ./src",
    "format:js": "eslint --fix",
    "check-types": "tsc --noEmit",
    "prebuild": "rm -rf ./dist",
    "build": "tsc -b ./tsconfig.cjs.json ./tsconfig.esm.json ./tsconfig.types.json",
    "postbuild": "echo '{\"type\": \"module\"}' > dist/esm/package.json&&echo '{\"type\": \"commonjs\"}' > dist/cjs/package.json",
    "pretest": "pnpm run build",
    "test": "node --test ./test/import*.test.*js&&c8 node --import tsx --test ./test/*.test.*",
    "test-watch": "pnpm run build&&node --import tsx --test --watch ./test/*.test.*",
    "gen-badge": "istanbul-cobertura-badger -e 85 -g 70 -r ./.nyc_output/cobertura-coverage.xml -d . -b cover"
  },
  "dependencies": {
    "webpack-bundle-analyzer": "4.9.1"
  },
  "devDependencies": {
    "@types/node": "22.5.1",
    "@espcom/eslint-config": "0.0.7",
    "tsx": "4.19.0",
    "c8": "10.1.2",
    "esbuild": "0.23.1",
    "lint-staged": "15.2.9",
    "typescript": "5.5.4",
    "husky": "9.1.5",
    "istanbul-cobertura-badger": "1.3.1"
  },
  "peerDependencies": {
    "esbuild": "^0.23.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "exports": {
    "types": "./dist/types/index.d.ts",
    "require": "./dist/cjs/index.js",
    "import": "./dist/esm/index.js"
  },
  "main": "dist/cjs/index.js",
  "types": "dist/types/index.d.ts",
  "packageManager": "pnpm@8.6.3"
}
